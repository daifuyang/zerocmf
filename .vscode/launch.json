{
    // 使用 IntelliSense 了解相关属性。 
    // 悬停以查看现有属性的描述。
    // 欲了解更多信息，请访问: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [   
        {
            "name": "zerocmf",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/server",
            "args": ["-f", "${workspaceFolder}/configs/config.yaml"],
            "env": {},
            "showLog": true,
            "trace": "verbose",
            "cwd": "${workspaceFolder}" // 设置 Debug 运行目录为项目根目录
        }
    ]
}